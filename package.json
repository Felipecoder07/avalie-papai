{
  "name": "arenix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:cliente": "npm run dev --prefix \"tela cliente\"",
    "dev": "concurrently -n \"backend,frontend,cliente\" -c \"blue,green,magenta\" \"npm:dev:backend\" \"npm:dev:frontend\" \"npm:dev:cliente\"",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:cliente": "npm run test --prefix \"tela cliente\"",
    "test": "npm run test:backend && npm run test:cliente && npm run test:frontend",
    "validate": "node scripts/validate.cjs",
    "validate:gateway": "npm run test --prefix backend -- tests/gateway.test.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
